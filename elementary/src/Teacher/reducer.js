import {

} from "./actions"

const initialState = {

}

export default function teacherState(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}