import {

} from "./actions"

const initialState = {

}

export default function studentState(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}